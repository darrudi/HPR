package ir.ac.itrc.qqa.semantic.enums;

/**
 * Defines what is the question focus in a plausible question
 * @author Ehsan Darrudi
 *
 */
public enum QuestionFocus
{
	ARGUMENT,		// we are looking for the missing argument
	REFERENT,		// we are looking for the missing referent
	DESCRIPTOR		// we are looking for the missing descriptor (not used at present)
}
